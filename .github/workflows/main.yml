on:
  push:
    branches:
      - main           ← only when pushing to main branch
      - develop        ← also trigger when pushing to develop branch
    paths:
      - 'backend/**'   ← ONLY trigger if files inside backend/ changed

